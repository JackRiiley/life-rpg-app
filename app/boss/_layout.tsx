import { Stack } from 'expo-router';
import React from 'react';

export default function BossLayout() {
  // This simple Stack will manage all screens
  // inside the 'boss' directory, like [id].tsx
  return <Stack />;
}